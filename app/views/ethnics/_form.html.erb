<%= form_with(model: ethnic, local: true) do |form| %>
  <% if ethnic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ethnic.errors.count, "error") %> prohibited this ethnic from being saved:</h2>

      <ul>
      <% ethnic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ethnic_name %>
    <%= form.text_field :ethnic_name, id: :ethnic_ethnic_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
