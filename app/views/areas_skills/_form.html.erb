<%= form_with(model: areas_skill, local: true) do |form| %>
  <% if areas_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(areas_skill.errors.count, "error") %> prohibited this areas_skill from being saved:</h2>

      <ul>
      <% areas_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :areas_skill_name %>
    <%= form.text_field :areas_skill_name, id: :areas_skill_areas_skill_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
