<%= form_with(model: skill, local: true) do |form| %>
  <% if skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

      <ul>
      <% skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :person_id %>
    <%= form.text_field :person_id, id: :skill_person_id %>
  </div>

  <div class="field">
    <%= form.label :areas_skill_id %>
    <%= form.text_field :areas_skill_id, id: :skill_areas_skill_id %>
  </div>

  <div class="field">
    <%= form.label :skill_name %>
    <%= form.text_field :skill_name, id: :skill_skill_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
